(()=>{
  let productTitle= 'My amazing product';
  // productTitle=null;
  // productTitle= ()=>{};
  // productTitle=123;

  productTitle='My amazing product changed';
  console.log('productTitle', productTitle);

  const productDescripcion="I'm bla bla bla blasas";
  console.log('productDescripcion',productDescripcion);

  const summary =`
  dasd
  dasd
  asd
  asd
  sad
  `;


  let productPrice = 100;
  let isNew: boolean=false;

  const summary1=`
  title: ${productTitle}
  descripcion: ${productDescripcion}
  price:${productPrice}
  isNew: ${isNew}
  `;

  console.log('summary1',summary1);

  const myString: string='';
})();
